# Agent Guide: Workflows

Workflows enforce the editorial contract.

## Rules

- Keep checks understandable from the Actions log.
- Badge checks should recognize only the approved badges from the root `AGENTS.md`.
- Link checks should use `.github/markdown-link-check.json`.
- Do not add network-heavy or flaky checks unless they protect a real repository rule.
