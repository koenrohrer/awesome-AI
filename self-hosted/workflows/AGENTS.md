# Agent Guide: Self-Hosted Workflows

This directory covers complete local-first workflows.

## Rules

- Prefer `[tested]` workflows that a maintainer ran end to end.
- Include the machine, runner, model, task, date, and observed caveats.
- Avoid "docker-compose and hope" entries that do not prove the loop works.
